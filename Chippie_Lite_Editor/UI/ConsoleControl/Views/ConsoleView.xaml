<UserControl x:Class="ConsoleControlLibrary.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ConsoleControlLibrary"
             mc:Ignorable="d"
             Background="{StaticResource Black}"
             IsHitTestVisible="True"
             SizeChanged="ConsoleView_OnSizeChanged"
             Loaded="ConsoleView_OnLoaded"
             d:DesignHeight="128" d:DesignWidth="128">
    
    <Grid x:Name="ConsoleGrid">
        <Canvas x:Name="ConsoleCanvas">
            <Rectangle x:Name="debugRect" Width="10" Height="10" Visibility="Visible" Canvas.ZIndex = "-1"/>
        </Canvas>
    </Grid>
</UserControl>
