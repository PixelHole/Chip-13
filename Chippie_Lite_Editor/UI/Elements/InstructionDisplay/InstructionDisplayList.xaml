<UserControl x:Class="Chippie_Lite_WPF.UI.Elements.InstructionDisplayList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent"
             d:DesignHeight="400" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Rectangle Fill="{StaticResource 'Deep Purple'}"/>
        
        <TextBlock Margin="4, 0, 0, 2" VerticalAlignment="Center" 
                   Text="Instructions" FontSize="22" FontFamily="{StaticResource TextFontAlt}"
                   Foreground="{StaticResource White}" Background="Transparent"/>
        
        <Border Grid.Row="1" BorderThickness="4, 0, 4, 4" BorderBrush="{StaticResource 'Deep Purple'}">
            <ScrollViewer x:Name="ScrollView" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          Style="{StaticResource StyledScrollViewer}">
                <StackPanel x:Name="InstructionList" Background="Transparent" Orientation="Vertical"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
