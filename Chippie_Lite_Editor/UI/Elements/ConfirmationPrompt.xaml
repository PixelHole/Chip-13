<Window x:Class="Chippie_Lite_WPF.UI.Elements.ConfirmationPrompt"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Chippie_Lite_WPF.UI.Elements"
             mc:Ignorable="d"
             AllowsTransparency="True"
             WindowStyle="None"
             WindowStartupLocation="CenterScreen"
             Background="{StaticResource 'Dark Blue'}"
             LostFocus="ConfirmationPrompt_OnLostFocus"
             MinHeight="96" MinWidth="64"
             MaxWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Rectangle Fill="{StaticResource Red}" MouseDown="DragBoxOnMouseDown"/>
            <TextBlock x:Name="TitleBlock" Background="Transparent" Foreground="{StaticResource White}"
                       FontFamily="{StaticResource TextFontAlt}" FontSize="22" TextWrapping="WrapWithOverflow"
                       VerticalAlignment="Center" HorizontalAlignment="Left"
                       Margin="4"/>
        </Grid>
        
        <Border Grid.Row="1" BorderThickness="4, 0 ,4 , 4" BorderBrush="{StaticResource Red}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="36"/>
                </Grid.RowDefinitions>
                
                <TextBlock x:Name="BodyBlock" Margin="4" 
                           Background="Transparent" Foreground="{StaticResource White}"
                           FontFamily="{StaticResource TextFontAlt}" FontSize="16" TextWrapping="WrapWithOverflow"/> 
                
                <StackPanel x:Name="ButtonsPanel" Grid.Row="1" Margin="4" Orientation="Horizontal" HorizontalAlignment="Center"/>
                
            </Grid>
        </Border>
    </Grid>
</Window>
