<UserControl x:Class="Chippie_Lite_WPF.UI.Windows.Development.CodeEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Chippie_Lite_WPF.UI.Windows.Development"
             mc:Ignorable="d"
             Background="Transparent"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
                      Background="Transparent" Style="{StaticResource StyledScrollViewer}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="64"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="InputBox" Grid.Column="1" Margin="4, 0 ,0 ,0"
                         Background="Transparent" Foreground="{StaticResource White}" 
                         BorderBrush="Transparent" BorderThickness="0" SelectionBrush="{StaticResource Pink}"
                         FontFamily="{StaticResource TextFont}" FontSize="22"
                         AcceptsReturn="True" AcceptsTab="True"/>
                
                <Border Grid.Column="0" BorderThickness="0, 0, 4, 0" BorderBrush="{StaticResource 'Faded Purple'}">
                    <TextBlock x:Name="LineDisplay" Background="{StaticResource 'Dark Blue'}" Margin="4, 0, 0, 0"
                               FontFamily="{StaticResource TextFont}" FontSize="22" Text="."
                               Foreground="{StaticResource White}"/>
                </Border>
            </Grid>
        </ScrollViewer>
        
        <Grid x:Name="CodeDataGrid" Grid.Row="1" Background="{StaticResource 'Deep Purple'}">
            <Label x:Name="LineCountDisplay" 
                   Background="Transparent" Foreground="{StaticResource White}"
                   FontFamily="{StaticResource TextFontAlt}" FontSize="16"
                   Content="LOC : 0"/>
        </Grid>
        
    </Grid>
</UserControl>